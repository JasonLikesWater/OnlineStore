# Online Store Back_End

## Prerequisites

be in the `Back_End` directory and install dependencies:

```bash
npm install
```

### important:
read the comment that starts on line 111 of users.js

## To run server

be in the `Back_End` directory and run:

```bash
npm start
```